<section id="contacto">
  <div class="contenedor-contacto">
    
    <div class="header-contacto">
        <span class="etiqueta">Hablemos</span>
        <h2>Contáctanos</h2>
    </div>

    <div class="datos-grid">
      
      <div class="dato-item">
        <div class="icon-wrapper wapp-icon">
            <svg xmlns="http://www.w3.org/2000/svg" width="35" height="35" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M3 21l1.65 -3.8a9 9 0 1 1 3.4 2.9l-5.05 .9"></path>
                <path d="M9 10a.5 .5 0 0 0 1 0v-1a.5 .5 0 0 0 -1 0v1a5 5 0 0 0 5 5h1a.5 .5 0 0 0 0 -1h-1a.5 .5 0 0 0 0 1"></path>
            </svg>
        </div>
        <h3>WhatsApp</h3>
        <p>+56 9 1234 5678</p>
        <a href="https://wa.me/56912345678" target="_blank" class="btn-neon-wapp">
          Hablar ahora
        </a>
      </div>

      <div class="dato-item">
        <div class="icon-wrapper mail-icon">
            <svg xmlns="http://www.w3.org/2000/svg" width="35" height="35" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <rect x="3" y="5" width="18" height="14" rx="2" ry="2"></rect>
                <polyline points="3 7 12 13 21 7"></polyline>
            </svg>
        </div>
        <h3>Correo</h3>
        <p>jaimepente@gmail.com</p>
        <a href="mailto:jaimepente@gmail.com" class="btn-neon-mail">
          Enviar correo
        </a>
      </div>

    </div>

  </div>
</section>

<style>
  #contacto {
    padding: 100px 20px;
    background-color: transparent; /* Hereda el fondo oscuro global */
    text-align: center;
  }

  .contenedor-contacto {
    max-width: 900px;
    margin: 0 auto;
  }

  .header-contacto {
    margin-bottom: 60px;
  }

  .etiqueta {
    color: #22d3ee;
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: 2px;
    font-size: 0.85rem;
    display: block;
    margin-bottom: 10px;
    text-shadow: 0 0 10px rgba(34, 211, 238, 0.3);
  }

  h2 {
    font-size: 3rem;
    color: #ffffff;
    margin: 0;
    font-weight: 800;
    text-transform: uppercase;
  }

  /* LA GRILLA DE CONTACTOS */
  .datos-grid {
    display: flex;
    justify-content: center;
    gap: 40px;
    flex-wrap: wrap; 
  }

  /* TARJETAS DE CRISTAL */
  .dato-item {
    background-color: rgba(30, 41, 59, 0.4); 
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255,255,255,0.05);
    padding: 40px 30px;
    border-radius: 24px;
    flex: 1;
    min-width: 280px;
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
    transition: transform 0.4s ease, border-color 0.4s ease;
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .dato-item:hover {
    transform: translateY(-5px);
    border-color: rgba(255,255,255,0.2);
  }

  /* CONTENEDOR DE ICONOS */
  .icon-wrapper {
      padding: 15px;
      border-radius: 50%;
      margin-bottom: 20px;
      display: flex;
  }

  .wapp-icon {
      background-color: rgba(37, 211, 102, 0.1);
      border: 1px solid rgba(37, 211, 102, 0.3);
      color: #25D366;
      filter: drop-shadow(0 0 8px rgba(37, 211, 102, 0.5));
  }

  .mail-icon {
      background-color: rgba(34, 211, 238, 0.1);
      border: 1px solid rgba(34, 211, 238, 0.3);
      color: #22d3ee;
      filter: drop-shadow(0 0 8px rgba(34, 211, 238, 0.5));
  }

  h3 {
    font-size: 1.5rem;
    color: #ffffff;
    margin: 0 0 10px 0;
    text-transform: uppercase;
    letter-spacing: 1px;
    font-weight: 700;
  }

  p {
    font-size: 1.1rem;
    color: #94a3b8;
    margin: 0 0 30px 0;
    font-weight: 500;
  }

  /* BOTONES NEÓN */
  .btn-neon-wapp, .btn-neon-mail {
    display: inline-block;
    background-color: transparent;
    padding: 12px 30px;
    border-radius: 50px;
    text-decoration: none;
    font-weight: bold;
    text-transform: uppercase;
    letter-spacing: 1px;
    transition: all 0.3s ease;
    width: 100%;
    box-sizing: border-box;
  }

  /* WhatsApp Neón */
  .btn-neon-wapp {
    color: #25D366;
    border: 2px solid #25D366;
  }

  .btn-neon-wapp:hover {
    background-color: #25D366;
    color: #0f172a;
    box-shadow: 0 0 20px rgba(37, 211, 102, 0.5);
    transform: scale(1.05);
  }

  /* Correo Neón */
  .btn-neon-mail {
    color: #22d3ee;
    border: 2px solid #22d3ee;
  }

  .btn-neon-mail:hover {
    background-color: #22d3ee;
    color: #0f172a;
    box-shadow: 0 0 20px rgba(34, 211, 238, 0.5);
    transform: scale(1.05);
  }

  /* CELULAR */
  @media (max-width: 768px) {
    #contacto { padding: 60px 10px; }
    h2 { font-size: 2.2rem; }
    .datos-grid { flex-direction: column; }
    .dato-item { padding: 30px 20px; }
  }
</style>