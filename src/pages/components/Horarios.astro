<section id="horarios">
    <h2>Nuestros Servicios</h2>

    <div class="horarios-grid">
        
        <div class="tarjeta martes">
                <div class="icon">📖</div>
                <h3>Martes</h3>
                <p class="hora">19:30 hrs</p>
                <p class="actividad">Culto Local Sigisfredo</p>
            </div>

            <div class="tarjeta jueves">
                <div class="icon">🔥</div>
                <h3>Jueves</h3>
                <p class="hora">19:30 hrs</p>
                <p class="actividad">Culto General</p>
            </div>

            <div class="tarjeta domingo">
                <div class="icon">🙌</div>
                <h3>Domingo</h3>
                <div class="bloque">
                    <p class="hora">10:00 hrs</p>
                    <p class="actividad">Escuela Dominical</p>
                </div>
                <div class="bloque">
                    <p class="hora">11:00 hrs</p>
                    <p class="actividad">Culto General</p>
                </div>
            </div>

    </div>
</section>

<style>
    /* 1. LA SECCIÓN GENERAL */
    #horarios {
        padding: 80px 20px; /* Espacio arriba/abajo y a los lados */
        background-color: #f3f4f6; /* Un gris muy clarito para diferenciar del fondo blanco */
        text-align: center; /* Centramos el título */
    }

    h2 {
        font-size: 2.5rem;
        margin-bottom: 50px;
        color: #111827;
        font-weight: 800;
    }

    /* 2. LA REJILLA (EL PADRE) */
    .horarios-grid {
        display: flex; /* Poner lado a lado */
        justify-content: center; /* Centrar el grupo en la pantalla */
        gap: 30px; /* Espacio entre tarjetas */
        max-width: 1100px; /* Que no se estiren demasiado en pantallas gigantes */
        margin: 0 auto; /* Centrar el bloque */
    }

    /* 3. LAS TARJETAS (LOS HIJOS) */
    .tarjeta {
        
        padding: 40px 30px;
        border-radius: 16px;
        flex: 1;
        min-width: 250px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.3); /* Sombra un poco más oscura */
        transition: transform 0.3s ease, box-shadow 0.3s ease;
        
        /* TEXTO BLANCO PARA QUE SE LEA SOBRE LA FOTO */
        color: white; 
        text-align: center;
        
        /* PREPARAR PARA FONDO */
        background-size: cover; /* La foto cubre todo */
        background-position: center; /* La foto se centra */
        position: relative; /* Necesario para efectos */
        overflow: hidden; /* Que nada se salga de los bordes redondos */
    }
    
    .tarjeta h3 {
        color: #fff; /* Título blanco */
        font-size: 1.8rem;
        text-shadow: 0 2px 4px rgba(0,0,0,0.5); /* Sombrita para leer mejor */
    }

    .tarjeta .hora {
        color: #60a5fa;
        font-weight: bold;
    }

    .tarjeta .actividad {
        color: #e5e7eb; /* Gris muy clarito casi blanco */
    }

    .tarjeta .icon {
        font-size: 3rem;
        text-shadow: 0 2px 5px rgba(0,0,0,0.3);
    }

    .martes {
        /* Foto de Biblia o Estudio */
        background-image: linear-gradient(rgba(0, 0, 0, 0.7), rgba(0, 0, 0, 0.7)), 
        url('/images/fondo-local.jpg');
    }

    .jueves {
        /* Foto de Adoración / Fuego */
        background-image: linear-gradient(rgba(0, 0, 0, 0.7), rgba(0, 0, 0, 0.6)), 
        url('/images/uno.jpg');
    }

    .domingo {
        /* Foto de Iglesia / Comunidad */
        background-image: linear-gradient(rgba(0, 0, 0, 0.7), rgba(0, 0, 0, 0.6)), 
        url('/images/fondo-domingo1.jpg');
    }

    /* Efecto Hover opcional: Aclarar un poco la imagen al pasar el mouse */
    .tarjeta:hover {
        transform: translateY(-10px);
        box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.5);
    }

    /* Efecto al pasar el mouse: Subir y más sombra */
    .tarjeta:hover {
        transform: translateY(-10px); /* Flota hacia arriba */
        box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
        border-top: 5px solid skyblue; /* Aparece una línea azul arriba */
    }

    /* ESTILOS DE TEXTO DENTRO DE LA TARJETA */
    .icon {
        font-size: 3rem;
        margin-bottom: 15px;
    }

    h3 {
        font-size: 1.5rem;
        color: #1f2937;
        margin-bottom: 15px;
        text-transform: uppercase;
        letter-spacing: 1px;
    }

    .hora {
        font-size: 1.25rem;
        font-weight: bold;
        color: #2563eb; /* Azul bonito */
        margin: 0;
    }

    .actividad {
        color: #4b5563; /* Gris oscuro */
        margin-top: 5px;
        font-size: 1.1rem;
    }

    .bloque {
        margin-bottom: 15px; /* Separar los dos horarios del domingo */
    }

    /* 4. RESPONSIVE (CELULAR) */
    @media (max-width: 768px) {
        .horarios-grid {
            flex-direction: column; /* Poner una debajo de la otra */
        }
        
        #horarios {
            padding: 50px 20px;
        }
    }
</style>